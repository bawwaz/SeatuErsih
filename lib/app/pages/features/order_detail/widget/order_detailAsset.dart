import 'package:flutter/material.dart';

class assetOrderDetail extends StatelessWidget {
  const assetOrderDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/img/driver.png'),
    );
  }
}
