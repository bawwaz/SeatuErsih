import 'package:flutter/material.dart';

class OrderDetailDesc extends StatelessWidget {
  const OrderDetailDesc({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}