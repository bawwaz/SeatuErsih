import 'package:flutter/material.dart';

class DeepCleanListView extends StatelessWidget {
  const DeepCleanListView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('DeepClean'),
      ),
    );
  }
}
